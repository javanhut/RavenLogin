[package]
name = "raven-lock"
description = "The RavenLinux lock screen. An ext-session-lock-v1 client: if it dies, the compositor keeps the screen locked rather than revealing the session."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
raven-greet-proto.workspace = true
raven-ui.workspace = true

anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
smithay-client-toolkit.workspace = true

[lints]
workspace = true
